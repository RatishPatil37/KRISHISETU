services:
  krishisetu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: krishisetu-app
    ports:
      - "8080:5000"
    env_file:
      - LokSevaAI_MERN/backend/.env
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
